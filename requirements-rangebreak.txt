## Only needed for the range-breakout backtester website (`python -m rangebreak`).
## Fully independent of requirements.txt / requirements-web.txt (the meme-coin bot) --
## this app shares no code or dependencies with that one beyond the pytest dev setup.

fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
httpx>=0.27,<1.0

## zoneinfo (stdlib, used for America/New_York session times) needs an IANA
## timezone database. Linux/Mac usually have one at the OS level already;
## Windows never does, so this is required there. Harmless everywhere else --
## zoneinfo prefers the OS copy when one exists and only falls back to this.
tzdata>=2024.1
